# flyway.toml

# More information on the parameters can be found here: https://documentation.red-gate.com/flyway/flyway-cli-and-api/configuration/parameters

# [environments.sample]
# url = "jdbc:h2:mem:db"
# user = "sample user"
# password = "sample password"
# # jarDirs = ["path/to/java/migrations"]
# # driver =
# # schemas =
# # connectRetries =
# # connectRetriesInterval =
# # initSql =
# # jdbcProperties =
# # resolvers =
# flyway.toml

# [flyway]
# locations = ["filesystem:./database/migrations"]

# [environments.flyway]
# url = "jdbc:postgresql://localhost/flyway_db"
# user = "postgres"
# password = "postgres"

# Database credentials
# flyway.user=postgres
# flyway.password=postgres-secret
# flyway.schemas=myschema
# flyway.url="jdbc:postgresql://localhost/flyway_db"

# [flyway.check]
# buildEnvironment = "build"

# Migration file configuration
# flyway.sqlMigrationPrefix=""
# flyway.sqlMigrationSeparator="_"
# flyway.sqlMigrationSuffixes=.sql
flyway.validateMigrationNaming=true

flyway.url=jdbc:postgresql://audio-archive-psql-db2.cxq8xikgucfb.us-east-2.rds.amazonaws.com:5432/audio_archive_production
flyway.user=postgres
flyway.password=postgres-secret

# SSL Configuration
flyway.connectRetries=10
flyway.initSql="SET sslmode='require'"
